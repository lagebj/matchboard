/**
 * OutcomePair — re-exported for discoverability under `touchline/viz/` (Touchline Design Atlas
 * `04_WIDGET_COMPONENT_CONTRACTS.md §3`). The real implementation is the one the first Touchline
 * pass already built at `src/components/touchline/evidence/outcome-pair.tsx` — not duplicated.
 */
export { OutcomePair } from "@/components/touchline/evidence/outcome-pair";
